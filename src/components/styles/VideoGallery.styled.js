import styled from 'styled-components'

export const StyledVideo = styled.div`
  max-width: 100%;
  margin: 0 64px;

  .video-container {
    width: 100%;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
  }

  .videos {
    line-height: 0;
    margin: 1rem;
    border-radius: 10px;
    overflow: hidden;
  }
`
